from setuptools import find_packages, setup

setup(
    name='busn30135',
    version='0.1.0',
    packages=find_packages(),
    description='Course utilities for BUSN30135',
    author='Student',
    install_requires=[
        # List any shared dependencies here if needed, e.g.
        # 'pandas',
    ],
)
